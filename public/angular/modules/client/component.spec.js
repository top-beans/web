'use strict';

describe('administrator', function () {

    beforeEach(module('administrator'));

    describe('AdministratorCtrl', function () {

        it('should ....', inject(function ($controller) {
            //spec body
            var ctrl = $controller('AdministratorCtrl');
            expect(ctrl).toBeDefined();
        }));

    });
});